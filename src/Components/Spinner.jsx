import React from 'react';

const Spinner = () => {
    return (
        <div>
            <span className="loading loading-spinner text-error"></span>
        </div>
    );
};

export default Spinner;